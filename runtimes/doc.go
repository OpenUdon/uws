// Package runtimes defines the UWS runtime supplement wire metadata.
//
// The supplement is metadata only. It names the portable x-uws-runtime
// operation extension and its typed JSON payload shape; execution remains the
// responsibility of a bound runtime.
package runtimes
