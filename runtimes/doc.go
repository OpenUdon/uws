// Package runtimes defines the UWS runtime supplement wire metadata.
//
// The supplement is metadata only. It names portable extension keys and typed
// JSON payload shapes used by runtime-owning UWS operations; execution remains
// the responsibility of a bound runtime.
package runtimes
